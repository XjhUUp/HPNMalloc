#include <vector>
#include <cstdio>
#include <algorithm>
#include <map>
#include <iostream>
#include <iomanip>  // 添加这一行，用于使用 std::fixed 和 std::setprecision
#include <limits.h>

using namespace std;

int main(int argc, char *argv[])
{
    int a=INT_MAX;
    a+=1;
    cout<<a<<endl;

}
